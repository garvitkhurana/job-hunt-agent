# What’s next (parked)

Do **not** build this until Phase 2 habit is real: daily board use + `hunt metrics` + outcomes for 1–2 weeks.

## Phase 2 — Use the loop (you)

- `hunt daily` → `hunt ui` → mark applied/skip
- `hunt outcome <id> interview|rejected|ghost` when you know
- Weekly: `hunt metrics` vs `output/metrics/baseline.json`
- Tune only `config.yaml` knobs if precision is bad

## Phase 3 — Form assist (separate plan later)

Paste-ready only; you still submit. Patterns from Linear / Revolut / Glean:

| Bucket | Examples | Approach |
|--------|----------|----------|
| Identity | name, email, LinkedIn | Profile defaults |
| Logistics | country, office, start date | Profile defaults |
| Legal | work auth, sponsorship | Profile facts — never invent |
| Judgment | compensation (no “negotiable”) | Stored target band |
| Narrative | AI tools / technical engagement | Experience-bank + LLM draft |
| Marketing | how did you hear / conference | Saved defaults |
| EEO / pronouns | voluntary | Skip or prefs |
| Consent / CAPTCHA | transcript AI, reCAPTCHA | Always human |

Order when building: **static defaults → narrative drafts → (much later) browser fill**. Never auto-submit.
